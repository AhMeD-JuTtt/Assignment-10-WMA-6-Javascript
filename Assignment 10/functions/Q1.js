// 1. Write a js program to find cube of any number using function.

function cube(num) {
    return num * num * num;
}

let numToFindCube = 5;
let result = cube(numToFindCube);
console.log(`The Cube of ${numToFindCube} is ${result}.`)